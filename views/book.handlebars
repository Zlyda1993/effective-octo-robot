<section class="painting">
  <img src="/images/{{book.img}}" alt="{{book.description}}">
  <div class="painting-details">
    <h3>{{book.title}}</h3>
    <p>By: {{book.author}}</p>
 
  </div>
</section>