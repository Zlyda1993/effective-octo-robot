<section class="card">
  <img src="/images/book_covers/{{book.filename}}" alt="book cover">
  <div>
    <h3>{{book.title}}</h3>
    <p>Genre: {{book.genre}}</p>
    <p>By: {{book.author}} in {{book.year}}</p>
  </div>
</section>
